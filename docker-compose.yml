services:
  fastapi-api:
    build: .            
    container_name: fastapi-api
    ports:
      - "8053:8053"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - shared_network
  
networks:
  shared_network:
    external: true